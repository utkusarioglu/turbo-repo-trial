{
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "rm -rf node_modules **/**/node_modules apps/android/android/app/build apps/web/.next"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "prettier": "^2.5.1",
    "turbo": "^1.9.3"
  },
  "name": "turbo-repo",
  "packageManager": "yarn@3.5.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
