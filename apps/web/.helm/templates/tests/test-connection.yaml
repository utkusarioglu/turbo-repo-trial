apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webClient.fullname" . }}-test-connection"
  labels:
    {{- include "webClient.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webClient.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
